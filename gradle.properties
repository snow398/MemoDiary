# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.java.home=D\:\\Android Studio\\jbr
android.useAndroidX=true
android.enableJetifier=true

# Kotlin settings
kotlin.code.style=official

# Configuration on demand can interfere with IDE imports and language server startup.
org.gradle.configureondemand=false

# Keep parallel execution enabled for normal builds.
org.gradle.parallel=true

# HTTP proxy (required for Gradle JVM to reach Maven repos)
systemProp.http.proxyHost=15.89.30.61
systemProp.http.proxyPort=8080
systemProp.https.proxyHost=15.89.30.61
systemProp.https.proxyPort=8080